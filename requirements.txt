# Python 3.12. Linux is the primary target — install with the `and-cuda`
# extra so TF pulls its own CUDA 12.x wheels and finds the NVIDIA GPU.
# On macOS/Windows-without-CUDA the bare `tensorflow` wheel runs CPU-only;
# on Apple Silicon, additionally `pip install tensorflow-metal==1.2.0`
# for Metal GPU support.
tensorflow[and-cuda]==2.18.1; sys_platform == "linux"
tensorflow==2.18.1; sys_platform != "linux"

biopython==1.87
numpy==2.0.2
pandas==3.0.2
scipy==1.17.1
scikit-learn==1.8.0
matplotlib==3.10.9
seaborn==0.13.2
timm==1.0.20
statsmodels==0.14.5

# Notebook execution (run_notebook.py uses nbclient; interactive work uses jupyter)
nbclient==0.10.4
ipykernel==7.2.0
notebook
