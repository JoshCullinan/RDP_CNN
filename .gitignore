dataRaw.zip
dataRaw/
/dataRaw
.venv
.claude

# Session artifacts
exec_log.txt
run_log.txt
exec_log_*.txt
eval_log_*.txt
cache_test_log_*.txt
__pycache__/

# Trained-model checkpoints — large + reproducible by re-running the notebook.
# If you want to ship a model snapshot to another machine, copy models_test/
# manually (or zip it). Tracked separately from source control.
models_test/
models_test_backup/

# load_dataset disk cache — regenerable from dataRaw/ via the notebook.
cache/

# Diagnostic figures — regenerate from the notebook / chart_eval.py.
figures/

#.tar files
models_test.tar
model_backup.tar