dataRaw.zip
dataRaw/
/dataRaw
.venv
.claude

# Session artifacts
exec_log.txt
run_log.txt
exec_log_*.txt
eval_log_*.txt
cache_test_log_*.txt
eval_run41_*.log
eval_run41_unseen_*.log
cache_test_run41_*.log
*.ipynb.bak_*
__pycache__/

# Trained-model checkpoints — large + reproducible by re-running the notebook.
# If you want to ship a model snapshot to another machine, copy models_test/
# manually (or zip it). Tracked separately from source control.
models_test/
models_test_backup/

# load_dataset disk cache — regenerable from dataRaw/ via the notebook.
cache/

# Diagnostic figures — regenerate from the notebook / chart_eval.py.
figures/

#.tar files
models_test.tar
model_backup.tar
history_*.json
# Run #42-era transient logs and status files
regen_*.log
cache_unseen_run42*.log
score_informative_*.log
copy_batch003_*.log
exec_log_run42c_*.txt
eval_run42c_*.log
REGEN_STATUS.txt
DATA_READY_STATUS.txt
regen_completion_history.log
regen_completion_watcher.sh
data_ready_watcher.sh

# One-shot patcher scripts (kept for reproducibility but not load-bearing)
_patch_*.py
_revert_*.py
get_diag_settings.py
get_path_A.py
get_re_apply_pathA.py
get_apply_run*.py
get_diag_run*.py

# Training / eval / audit log artifacts (regeneratable)
eval_*.log
train_*.log
train_*.log.prior_attempt_oom
audit_*.log
build_runB2_*.log
build_full.log
step4_pipeline.log
m12_*.log
m14_*.log
m04_*.log
splits/*.log

# Investigation work-product (the .log is regenerable from the .py)
m13_santa_vs_real.log
m14_realism_measures.partial.json

# Local thesis PDF — personal artifact, not project code
Detecting Viral Recombination*.pdf

# SANTA simulator — keep XMLs/scripts/Simulation.py in git, exclude binary + derived
santaSim_RDP/outputs/
santaSim_RDP/santa.jar
santaSim_RDP/sarbeco.fas
santaSim_RDP/sarbecoStart.fa
santaSim_RDP/.vscode/

# LANL parent-pairing expansion (Task 8): regenerable via
# `python -m spectrogram.expand_lanl` (network + MAFFT). Kept out of git so
# load_lanl_triplets()'s "prefer expanded when present" default can't
# silently break the Task-1 4-CRF test/gate for other contributors.
data/lanl_crf/triplets_expanded/
